from typing import Any, Dict


def write_yaml(obj: Dict[str, Any], path: str) -> None:
	"""Schreibt ein Objekt als YAML in eine Datei.

	Input: obj (dict), path (str)
	Output: None
	# to do: Verwende yaml.safe_dump, sichere atomar und behandle IO-Fehler.
	"""
	raise NotImplementedError("# to do: write_yaml implementieren")

