from typing import Any, Dict


def read_yaml(path: str) -> Dict[str, Any]:
	"""Liest eine YAML-Datei und gibt das geladene Objekt zurück.

	Input: path (str)
	Output: Dict/Objekt entsprechend YAML-Inhalt
	# to do: Implementiere sicheres Lesen (ruft yaml.safe_load), Error-Handling und Encoding.
	"""
	raise NotImplementedError("# to do: read_yaml implementieren")

